name: Deploy NestJS via SSH + PM2

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up SSH key and agent
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Add host to known_hosts
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -H "${{ secrets.SSH_HOST }}" >> ~/.ssh/known_hosts
        shell: bash

      - name: SSH & Deploy
        run: |
          ssh -o StrictHostKeyChecking=no ${{ secrets.SSH_USERNAME }}@${{ secrets.SSH_HOST }} << 'EOF'
            cd /root/nodejs/nestjs-prisma

            export NVM_DIR="$HOME/.nvm"
            [ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh"

            node -v
            npm -v
            pm2 -v

            # Create .env file directly on VPS (Fixed heredoc)                                               
            printf '%s\n' '${{ secrets.ENV }}' > .env   

            # Secure the file                                                                                
            chmod 600 .env     

            # Verify .env was created correctly                                                       
            echo "✅ Environment file created with $(wc -l < .env) lines" 

            git pull origin main
            npm ci
            npm run build
            npm run generate
            npm run migrate:prod
            pm2 startOrReload ecosystem.config.js
          EOF
        shell: bash
